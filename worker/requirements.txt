redis==5.0.1
minio==7.2.7
numpy==1.26.4
python-dotenv==1.0.0
psycopg2-binary==2.9.9

# ── 메시 변환 ──────────────────────────────
trimesh==4.4.3
pygltflib==1.16.1
scipy==1.13.1
networkx==3.3

# ── CAD 포맷 변환 (STEP, IGES, BREP → GLB) ─
cadquery==2.4.0

# ── 포인트 클라우드 ─────────────────────────
laspy[laszip]==2.5.3
open3d==0.18.0

# ── 영상 프레임 추출 (NeRF/3DGS 전처리) ────
opencv-python-headless==4.10.0.84

# ── 압축 해제 ────────────────────────────────
# zipfile은 stdlib 포함
